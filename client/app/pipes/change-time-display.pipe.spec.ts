import { ChangeTimeDisplayPipe } from './change-time-display.pipe';

describe('ChangeTimeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeTimeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
