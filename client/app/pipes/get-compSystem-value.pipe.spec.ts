import { GetCompSystemValuePipe } from './get-compSystem-value.pipe';

describe('GetCompSystemValuePipe', () => {
  it('create an instance', () => {
    const pipe = new GetCompSystemValuePipe();
    expect(pipe).toBeTruthy();
  });
});
