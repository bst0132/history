import { GetSportValuePipe } from './get-sport-value.pipe';

describe('GetSportValuePipe', () => {
  it('create an instance', () => {
    const pipe = new GetSportValuePipe();
    expect(pipe).toBeTruthy();
  });
});
